import React from 'react'
import styles from './DisplayMovies.module.css'

type Props = {}

const DisplayMovies = (props: Props) => {
  return (
    <div className={styles.display}>DisplayMovies</div>
  )
}

export default DisplayMovies